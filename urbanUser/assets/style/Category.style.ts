import { ColorScheme } from "@/hooks/theme/Themes";
import { StyleSheet } from "react-native";

const createCategoryStyle = (colors: ColorScheme) => {
    const styles = StyleSheet.create({

    });
    return styles;
}