import { ColorScheme } from "@/hooks/theme/Themes";
import { StyleSheet } from "react-native";

const createHomeStyle = (colors: ColorScheme) => {
    const styles = StyleSheet.create({

    });
    return styles;
}